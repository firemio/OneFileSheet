{
  "name": "one-file-sheet",
  "private": true,
  "description": "Build tooling for OneFileSheet. Dev-only dependencies - the app itself has zero runtime dependencies.",
  "scripts": {
    "build": "node build.js"
  },
  "devDependencies": {
    "csso": "^5.0.5",
    "terser": "^5.31.0"
  }
}
